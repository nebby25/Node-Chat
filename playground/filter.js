var arr = ['Ben', 'Taylor', 'Jerry', 'Tomlin', 'Savage'];

var user = arr.filter((name)=> na)


//        var user = this.getUser(id);
//        if(user) {
//           this.users = this.users.filter((user)=> user.id !== id); 
//        }
//        return user;

//return this.users.filter((user)=> user.id === id)[0];